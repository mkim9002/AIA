import autokeras as ak
from keras.datasets import mnist
print(ak.__version__)

import tensorflow as tf
import time

#1.데이터
(x_train, y_train),(x_test, y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.lad_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()
(x_train,y_train),(x_test,y_test)= mnist.load_data()

model = ak.ImageClassifier(
    overwrite=False,
    max_trials=2,
)

model = ak.ImageClassifier(
    overwrite=False,
    max_trials=2
)
model = ak.ImageClassifier(
    overwrite=False,
    max_trials=2
)
model = ak.ImageClassifier(
    overwrite=False,
    max_trials=2
)
model = ak.ImageClassifier(
    overwrite=False,
    max_trials=2
)
model = ak.ImageClassifier(
    overwrite=False,
    max_trials=2
)
model =ak.ImageClassifier(
    overwrite=False
    max_trials=2
)
model = ak.ImageClassifier(
    overwrite=False,
    max_trials=2
)
model = ak.ImageClassifier(
    overwrite=False
    max_trials=2
)
model = ak.ImageClassifier(
    overwrite=False
    max_trials=2
)

moedl = ak.ImageClassifier(
    overwrite=False,
    max_trials=2
)

model = ak.ImageClassifier(
    overwrite=False,
    max_trials=2
)
